import { jest } from '@jest/globals'

describe('gradle.ts', () => {
  beforeEach(() => {
    jest.clearAllMocks()
  })

  describe('hoge', () => {
    it('hoge', () => {
      expect(1).toEqual(1)
    })
  })
})
